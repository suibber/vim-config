# vim-config
```
// 备份原来的内容，移动路径
cd ~
mv .vim .vim_bak
git clone https://github.com/suibber/vim-config.git ~/.vim
git clone https://github.com/gmarik/Vundle.vim.git ~/.vim/plugin/Vundle.vim
// 打开vim，执行:PluginInstall
// done
